# ---- Market data (data only; this bot never places orders) ----
ALPACA_API_KEY=
ALPACA_SECRET_KEY=

# ---- NEW dedicated Telegram bot for manual signals ----
# Create a fresh bot with @BotFather, then message it once and hit
# /telegram-setup to auto-detect the chat id.
MANUAL_TELEGRAM_TOKEN=
MANUAL_TELEGRAM_CHAT_ID=

# ---- Optional email mirror (Resend over HTTPS; Railway blocks SMTP) ----
RESEND_API_KEY=
EMAIL_TO=
EMAIL_FROM=onboarding@resend.dev

# ---- Optional news digest ----
FINNHUB_KEY=
GROQ_API_KEY=

# ---- Optional ----
STATE_DIR=memory
PORT=8080
